# Copy ไฟล์นี้เป็น .env แล้วใส่ค่าจริง
# บน Railway ใส่ใน Variables tab แทน ไม่ต้องสร้าง .env

DISCORD_TOKEN=ใส่ token จาก Discord Developer Portal
VOICE_LOG_CHANNEL_ID=ใส่ Channel ID ที่ต้องการ log
DASHBOARD_PASSWORD=ใส่ password สำหรับ dashboard (ถ้าไม่ใส่ = ไม่มี auth)
